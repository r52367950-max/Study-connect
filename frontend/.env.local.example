# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:3000

# Optional: Override the app name
NEXT_PUBLIC_APP_NAME=StudyConnect

# DirB shell (sidebar + topbar). Set to "false" to fall back to the classic navbar layout.
NEXT_PUBLIC_DIRB_ENABLED=true
